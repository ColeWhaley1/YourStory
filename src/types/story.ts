export interface StoryInfo {
    id: string,
    title: string,
    author: string,
    img: string,
    rating: number,
}

export interface Story {
    author_id: string,
    description: string,
    rating: number,
    story_text: string,
    thumbnail: string,
    title: string,
}